<div style="width: 1100px;margin: 0 auto;">
<br />
<%=link_to(image_tag("get_the_app.png", :class => "appAd"),"about.html") %>
<%= image_tag("FounditLogo@2x.png", :class => "mainLogo") %>
<br /><br />
<div style="margin: 0 auto;width: auto;padding-left: 36%;">
	<%= link_to "New Posting", new_posting_path, :class => "homeText" %>
</div>
<br /><br />
</div>
<table style="width: 75%;margin: 0 auto;" border="0" cellspacing='0'>
  <tr>
  	<th>ID</th>
  	<th>Image</th>
  	<th>Type</th>
    <th>Item</th>
    <th>Created At</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @postings.each do |posting| %>
  <tr>
  	<td><%= posting.id %></td>
  	<td><%= image_tag(posting.photo.url(:thumb)) %></td>
  	<td>
  		<%= 'Lost' if posting.posting_type == "1" %>
  		<%= 'Found' if posting.posting_type == "2" %>
  	</td>
    <td><%= posting.name %></td>
    <td><%= posting.created_at_formatted %></td>
    <td><%= link_to 'Show', posting %></td>
    <td><%= link_to 'Edit', edit_posting_path(posting) %></td>
    <td><%= link_to 'Destroy', posting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
</div>
